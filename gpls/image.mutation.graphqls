extend type Mutation {
    addSubImage(addSubImageInput: AddSubImageInput): Result
    deleteImage(path: String): Result
}

input AddSubImageInput {
    id: String,
    category: Category,
    url: String,
    logAtStr: String
    subId: Int,
    width: Int
}
